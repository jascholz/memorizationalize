.header--banner
  -# = link_to header_title, root_path, class: 'header--title', 'up-target': '.content, [settings-toggle]', 'up-restore-scroll': true
  = link_to header_title, nil, class: 'header--title'

  .header--links
    - if user_signed_in?
      .header--sign-out
        = link_to "Abmelden", sessions_path, method: :delete, class: 'header--link', 'up-target': 'body'
      = link_to nil, class: 'header--icon', 'settings-toggle': '' do
        = render 'svg/settings'

- if user_signed_in?
  = render 'home/forms'
